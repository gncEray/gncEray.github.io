// Eray Genç
// 19050111005			
$(document).ready(function() {
	$("#accordion").accordion({
		collapsible: true,  // allows panels to be both opened and closed
		active: false  //  starting all panels closed
	});
});
